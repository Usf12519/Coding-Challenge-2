// Task 1: Calculating the Tip

    if else( bill >= 50 && bill <= 500 ? * 0.15 :
        bill * 0.20);

    if else (bill >= 300{
        tip = 0.2)
        else{ 
            tip =0.2);

            return (tip * bill);


        }
    

    